#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){

    ll H,W;
    cin >> H >> W;

    std::vector<string> v(H);
    for(ll i = 0; i < H ;i++){
      cin >> v[i];
    }
    for(ll i = 0; i < H; i++){
      cout << v[i] << endl;
      cout << v[i] << endl;;
    }

}
