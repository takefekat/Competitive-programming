#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
using vll = vector<ll>;
using vvll = vector<vll>;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()

const long long MOD = 998244353;

void solve(long long R, long long G, long long B, long long K){

}

int main(){
    long long R;
    std::scanf("%lld", &R);
    long long G;
    std::scanf("%lld", &G);
    long long B;
    std::scanf("%lld", &B);
    long long K;
    std::scanf("%lld", &K);
    solve(R, G, B, K);
    return 0;
}
