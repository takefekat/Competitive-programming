#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(long long x)
{

    cout << (x == 0 ? 1 : 0) << endl;
}

signed main()
{
    long long x;
    scanf("%lld", &x);
    solve(x);
    return 0;
}
