#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
using vll = vector<ll>;
using vvll = vector<vll>;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()


void solve(long long N, long long P, std::string S){

}

int main(){
    long long N;
    std::scanf("%lld", &N);
    long long P;
    std::scanf("%lld", &P);
    std::string S;
    std::cin >> S;
    solve(N, P, S);
    return 0;
}
