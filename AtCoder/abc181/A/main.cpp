#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(long long N)
{
    cout << (N % 2 == 0 ? "White" : "Black") << endl;
}

signed main()
{
    long long N;
    scanf("%lld", &N);
    solve(N);
    return 0;
}
