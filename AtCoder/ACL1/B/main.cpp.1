#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<vector<ll>> graph;

int main()
{
    // in
    ll N;
    cin >> N;

    //solve
    // k*(k+1)/2 = x*N
    // k(k+1) = y N (2x = y)

    return 0;
}