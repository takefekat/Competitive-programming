#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(long long N, long long W)
{
    cout << N / W << endl;
}

signed main()
{
    long long N;
    scanf("%lld", &N);
    long long W;
    scanf("%lld", &W);
    solve(N, W);
    return 0;
}
