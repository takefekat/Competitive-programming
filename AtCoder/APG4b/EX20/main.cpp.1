#include <bits/stdc++.h>
using namespace std;
typedef long long ll;



void solve(long long N, std::vector<long long> p){

}

signed main(){
    long long N;
    scanf("%lld",&N);
    std::vector<long long> p(N-1);
    for(int i = 0 ; i < N-1 ; i++){
        scanf("%lld",&p[i]);
    }
    solve(N, std::move(p));
    return 0;
}
