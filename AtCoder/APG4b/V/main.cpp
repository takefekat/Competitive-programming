#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<ll> vll;
typedef vector<vll> vvll;

signed main() {
  ll N;
  cin >> N;
  vll p(N);
  for (ll i = 0; i < N; i++) {
    cin >> p[i];
  }

  return 0;
}
