#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
using vll = vector<ll>;
using vvll = vector<vll>;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()


void solve(std::vector<long long> t){

}

int main(){
    std::vector<long long> t(8);
    for(int i = 0 ; i < 8 ; i++){
        std::scanf("%lld", &t[i]);
    }
    solve(std::move(t));
    return 0;
}
