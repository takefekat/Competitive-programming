#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(std::string S)
{
    cout << (S == "ABC" ? "ARC" : "ABC") << endl;
}

signed main()
{
    std::string S;
    std::cin >> S;
    solve(S);
    return 0;
}
