#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(long long S, long long W)
{

    cout << (S > W ? "safe" : "unsafe") << endl;
}

signed main()
{
    long long S;
    scanf("%lld", &S);
    long long W;
    scanf("%lld", &W);
    solve(S, W);
    return
}
