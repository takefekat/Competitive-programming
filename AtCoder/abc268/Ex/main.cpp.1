#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
using vll = vector<ll>;
using vvll = vector<vll>;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()


void solve(std::string S, long long N, std::vector<std::string> T){

}

int main(){
    std::string S;
    std::cin >> S;
    long long N;
    std::scanf("%lld", &N);
    std::vector<std::string> T(N);
    for(int i = 0 ; i < N ; i++){
        std::cin >> T[i];
    }
    solve(S, N, std::move(T));
    return 0;
}
