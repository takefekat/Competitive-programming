#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


const long long MOD = 1000000007;
vector<ll> fact;


void solve(long long N, long long M){

    // M個からN個選ぶ組合せの数
    for(ll i=0; i<N; i++) {
        if(  ) {
            
        }        
    }

}

signed main(){
    long long N;
    scanf("%lld",&N);
    long long M;
    scanf("%lld",&M);
    solve(N, M);
    return 0;
}
